fastapi==0.115.*
uvicorn[standard]==0.34.*
httpx==0.28.*
supabase==2.*
apscheduler==3.*
pyyaml==6.*
python-dotenv==1.*
anthropic>=1.2.0,<2  # structured outputs (output_config.format) requires >=1.2.0 - verified against geoai.py
pandas>=2
numpy>=1.26,<3
scikit-learn>=1.4,<2
scipy>=1.11,<2
lightgbm>=4

# Delhi Open Transit Data (OTD) GTFS-realtime decoding (audit/context-layer
# only - see app/delhi_otd.py). Tiny, pure-Python bindings + protobuf;
# doesn't touch any existing dependency's pinned version.
gtfs-realtime-bindings>=2

# Error tracking — optional. Initialised in main.py when SENTRY_DSN is set.
sentry-sdk[fastapi]>=2

# OSM road network parsing — isrm_osm_roads.py (ISRM dispersion kernel source layer).
# Official OSM Foundation Python bindings; pre-built wheels for Python 3.11 Linux.
# No geopandas/GDAL dependency chain — ships its own libosmium.
osmium>=3.7

# dev / tests only
pytest>=8
